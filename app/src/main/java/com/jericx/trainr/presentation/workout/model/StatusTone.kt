package com.jericx.trainr.presentation.workout.model

// The three colours a status chip can take. Weeks and workouts have different
// vocabularies but share these tones, so the chip maps tone to token once.
enum class StatusTone {
    DONE,
    ACTIVE,
    IDLE
}
